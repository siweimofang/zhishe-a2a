# 【RN14 人→入族 全量执行件 交底】2026-09-07

主令:【RN14人→入族全量执行令】(2026-09-07 拍板) + 执行令补丁(s1九条double_lir_mg现态定谳, 判定线20:56固化)
结论: **五段全绿 (fixlist→ledger→gen→apply→verify→ground), commit 已双推三端对齐。**

---

## 一、执行件路径

| 产物 | 路径 |
|---|---|
| 执行清单 fixlist (权威件) | D:\projects\非默认outputs\修复微批_人入族1444op_fixlist_20260907.json (153116 B, 与workspace rn_patch14_fixlist.json 字节全等) |
| 全量台账 ledger (1454条逐条账) | D:\projects\非默认outputs\人入族全量执行ledger_20260907.json |
| pre态基线快照 | D:\projects\非默认outputs\人入族pre_state快照_20260907.json |
| 生成器 / 解析器 | workspace: gen_rn14_fixlist.py / rn14_resolve.py (v3七级锚定阶梯+置换集不变性位点指派) |
| apply / verify | workspace: kb_apply.py (复用) / verify_rn14.py (独立盘上回读) |
| 报告 | rn14_gen_report.txt / rn14_verify_report.txt / kb_preRN14_log.txt |

注: 目录内同名 `修复微批_人入族1446op_fixlist_20260907.json` 为执行令补丁到达前的中间版(双字位拆2op), 已被 1444op 版取代, 以 1444op 版为权威。

## 二、apply 计数 (三类分列, 对账 1454)

| 类 | 条数 | 明细 |
|---|---|---|
| **执行 EXEC** | **1444** | 逐gid逐ctx锚定, op=最小唯一窗口 count==1; 预检三态判定全为"未执行过"(全锚 count==1) 正常执行 |
| **拦截 BLOCKED (合法已终态)** | **9** | s1 八条 double_lir_mg (k3963/k10716/k11657/k12097/k12358/k17249/k18816/k21665): 埋理0907批已一步修到位, 现态埋入=1, 锚全0=拦截——与补丁定谳逐条一致; + k13005 (maili顺手人→入已修) |
| **跳过 SKIP_DUP** | **1** | s3:001240 = s2:000517 跨片重复申报 (k16483 装人同位, census行全等), 记录直加不重复执行 |

另有 s2-5 判定单 `skip_special` 48跨片dup + 4片内同位 + 11存疑: 按主令**一律不执行**; 11条 uncertain 与 BACKLOG_NEW 6条词目候选: **本批不入库** (蒸馏禁区)。 Records 对账: 1444 + 9 + 1 = **1454 ✓**

## 三、执行要点 (补丁合规)

- **k18043 (补丁第2条)**: 单 op `理人→埋入` 一步到位, 锚=现态在场「理人」count=1, 未换锚「埋人」。post 实测: 理人=0, 埋人=0, 埋入=1 ✓ (补丁 post 终验第1项)
- **8条double终态**: post 实测逐条 理人=0/埋人=0/埋入=1, 维持不变 ✓ (补丁 post 终验第2项)
- 抽样病灶位人→入生效: 29 spots 全对 (清理入库/沉入孔内/细致入微/锤入/灯泡旋入/总入口/压入木台豁口/充入惰性/喷入×2 等) ✓ (补丁 post 终验第3项)
- 手术定谳 3 条带硬断言: s2:000713 总人口→总入口(k18317) / s3:000929 插人→插入(k4185, census行证判定单首段误转写) / s6:002799 压人→压入(k12559, census窗口偏移16定谳第一处, 第二处压人部分眼筛基准不动)
- 双字位并簇: k18043 理人→埋入 与 k15098 充人情→充入惰 均单 op 一步到位 (相邻簇合并, 唯一性由包含唯一子窗保证)

## 四、门禁实录 (全绿)

1. **gen**: pre门(HEAD=2db593aa/blob=8858ac86/字节=39248524/entries=29788/重dump字节全等) → 指派穷尽(无SURFACE残留) → 位点对账 {人→入:1444, 理→埋:1, 情→惰:1} = 1446点 → 聚类唯一窗口(fields=1057) → 顺序仿真+**位点重derive门**(sim逐字符==pre在位点上的精确替换, 替代对跨界token失效的算术门) → 回归门+28spots → fixlist双路落盘字节全等
2. **apply**: 三态预检(1444 op 全 count==1) → BAK=knowledge_backup_20260907_preRN14.json(39248524B) → 逐op执行 → post校验(旧值除/新值在, **未触碰28737条目逐条全等**) → 原子写 os.replace → ground1(HEAD blob→CRLF==pre盘) / ground2(BAK+fixlist序替重放==post盘字节) 双绿
3. **verify (独立盘上回读)**: 格式保真(bytes不变39248524, indent=1, CRLF, 无BOM, 末尾无换行, entries=29788) + BAK sha256 == 判定线pre基线 d727df30...77507838 + 1444op旧值除新值在 + 29spots + 字量守恒(人−1444/入+1444/理−1/埋+1/情−1/惰+1) + 未触碰28737条逐条全等 → **RN14_VERIFY_GREEN**
4. **ground**: `git diff --numstat` = **1057/1057** 行 (1057个修改字段), HEAD blob→CRLF == pre盘

## 五、commit 与双推

- commit: **614a5c1** `fix(知识库): 人→入族1444op修复微批(1051条目1446点, entries不变)` (仅 add data/knowledge.json, 1 file changed, 1057 insertions(+), 1057 deletions(-))
- post blob 对象ID: **40afa17a** (pre: 8858ac86)
- 推送: **Gitee(origin) 先推 ✓ → GitHub 后推 ✓** (均 2db593a..614a5c1 master->master)

## 六、三端终验

| 端 | master refs/heads |
|---|---|
| 本地 HEAD | 614a5c1a56dae77fd032a332a5b2f20f5daee261 |
| Gitee (origin) | 614a5c1a56dae77fd032a332a5b2f20f5daee261 ✓ |
| GitHub | 614a5c1a56dae77fd032a332a5b2f20f5daee261 ✓ |

GitHub ls-remote 中途网络抖动(连接重置×5), 稍候恢复后复验通过; 期间以 push 协议确认行+raw端点HTTP 200 辅证, API 403 为共享IP限流非连通性问题。

## 七、挂账与本批口径内审计

1. **埋理族残留挂账 (新发现, 不属RN14范围, 建议埋理补漏微批)**: 埋理0907批为「窗口式单点替换」, 同条目/同bucket外仍有漏网——`预理`×3: k4347「预理工作」/ k12427「预理工作」/ k18474「预留、预理」; `理设`×2: k3675「预埋件在**理设**前」/ k17349「在**理设**管线时」。均非已挂疑点(疑点仅 k4224 预理孔洞1处)。现态绝对值: 预理=4(3漏网+1疑点), 理设=61(2漏网+59合法子串如合理设计/处理设备/监理设施)。RN14 回归门已锁定本批未触碰这些词。
2. **「人人」66→53 审计留档**: −13 处全部为词首人→入的跨界合法消耗(深入**人**心→深入**入**心形字面: 深入人心×4 / 射入人眼×2 / 让人入眠 / 浸入人的 / 侵入人体 / 工人入场×3), 逐处context核验正确, 见 rn14_regr_debug.txt; 其余10个回归词严格不变。
3. 工具层教训(留档): 跨行劈词位点的「old内偏移→raw偏移」必须经 sidx 映射(000672实证); 最小唯一窗口场景下窗口delta算术门对跨界token失效, 以位点重derive充要门替代。

---
KB会话 (knowledge.json 单写手) 2026-09-07
